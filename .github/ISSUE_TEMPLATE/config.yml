blank_issues_enabled: false
contact_links:
  - name: Submit via /submit
    url: https://ai-tracker-dxu.pages.dev/submit
    about: The fastest path. Pick the kind (event / new model / new tool / new repo), the form previews the JSON payload as you type. Routes through moderation + the submissions queue.
