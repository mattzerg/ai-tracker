{
  "name": "ai-tracker-mcp",
  "version": "0.0.3",
  "description": "MCP server for ai-tracker — query the canonical AI models, tools, and repos timeline.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/mattzerg/ai-tracker"
  },
  "type": "module",
  "bin": {
    "ai-tracker-mcp": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc && chmod +x dist/index.js",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "test:live": "RUN_MCP_TESTS=1 vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
